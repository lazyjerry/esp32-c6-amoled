// 由 scripts/gen-sound.sh 產生，請勿手動編輯
#pragma once

#include <stddef.h>
#include <stdint.h>

#define SND_SAMPLE_RATE 16000

extern const int16_t snd_clack[];
extern const size_t snd_clack_len;
extern const int16_t snd_bell[];
extern const size_t snd_bell_len;
